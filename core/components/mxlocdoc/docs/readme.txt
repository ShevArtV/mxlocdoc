mxLocDoc
========

mxLocDoc is a lightweight MODX Revolution 2 extra for reading local Markdown
documentation inside the MODX manager.

The package is currently in early development. The v1 scope includes a manager
CMP, manifest navigation, safe Markdown rendering, protected asset delivery and
search over local .md files.
