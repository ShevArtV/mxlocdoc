# mxLocDoc Documentation

This folder is the default documentation root for mxLocDoc.

Set the `mxlocdoc.docs_path` system setting to another local Markdown folder when a project keeps its docs elsewhere. The setting supports `[[+corePath]]`, for example:

```text
[[+corePath]]components/mxlocdoc/docs/
```
